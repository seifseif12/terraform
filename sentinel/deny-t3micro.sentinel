import "tfplan/v2" as tfplan
import "strings"

main = rule {
  all tfplan.resource_changes as r {
    r.type is "aws_instance" and
    all r.change.actions as action { action in ["create", "update"] } and
    not strings.lower(r.change.after.instance_type) == "t3.micro"
  }
}
